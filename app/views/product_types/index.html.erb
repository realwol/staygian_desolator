<p id="notice"><%= notice %></p>

<h2>产品分类</h2>

<table class="table">
  <thead>
    <tr>
      <th>编号</th>
      <th>名称</th>
      <th>父节点编号</th>
      <th colspan="3">操作</th>
    </tr>
  </thead>

  <tbody>
    <% @product_types.each do |product_type| %>
      <tr>
        <td><%= product_type.id %></td>
        <td><%= product_type.name %></td>
        <td><%= product_type.father_node %></td>
        <td><%= link_to 'Show', product_type %></td>
        <td><%= link_to 'Edit', edit_product_type_path(product_type) %></td>
        <td><%= link_to 'Destroy', product_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<hr>

<ul id="productTypeMenu" style="width:50px;">
  <li>Test</li>
  <li>Hello
    <ul>
      <li>Test1
        <ul>
          <li class="leafPoint">Test2</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>
<hr>

<%= link_to 'New', new_product_type_path %>

<script type="text/javascript">
$(document).ready(function(){
  $('#productTypeMenu').menu();

  $('.leafPoint').click(function(){
    alert(1);
  });
});
</script>
