<h5>待审核产品列表：<%= @products.total_count %></h5>

<table class='table table-bordered' style="width:90%;">
  <thead>
    <tr>
      <th>产品店铺</th>
      <th>产品品牌</th>
      <th>产品标题</th>
      <th>SKU</th>
      <th>副SKU</th>
      <th>产品号码</th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.shop.try(:name) %></td>
        <td><%= product.brand.try(:name) %></td>
        <td><%= product.title %></td>
        <td><%= product.sku %></td>
        <td><%= product.sku1 %></td>
        <td><%= product.product_number %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @products %>
